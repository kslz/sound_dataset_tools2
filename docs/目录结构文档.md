大概，之后还会改

```
project_root/
│
├── presentation/          # UI界面相关代码
│   ├── main_window.py     # 主界面相关逻辑
│   ├── task_queue_page.py # 任务队列页面相关逻辑
│   └── ...                # 其他界面文件
│
├── application/           # 业务逻辑处理
│   ├── services/          # 业务服务模块
│   │   ├── optimization_service.py  # 优化逻辑服务
│   │   ├── evaluation_service.py    # 评测服务
│   │   ├── speaker_recognition_service.py  # 发音人识别服务
│   │   └── ...              # 其他服务文件
│   ├── task_queue.py       # 任务队列管理
│   ├── exceptions.py       # 自定义异常类
│   └── ...                # 其他应用逻辑文件
│
├── domain/                # 领域模型和业务实体
│   ├── models/            # 领域模型类
│   │   ├── voice_data.py   # 语音数据模型
│   │   ├── evaluation_result.py   # 评测结果模型
│   │   ├── speaker_info.py   # 发音人信息模型
│   │   └── ...             # 其他模型文件
│   ├── services/          # 领域服务接口和实现
│   │   ├── optimization_service_protocol.py  # 优化逻辑协议定义
│   │   ├── evaluation_service_protocol.py    # 评测服务协议定义
│   │   ├── speaker_recognition_service_protocol.py  # 发音人识别服务协议定义
│   │   └── ...             # 其他服务接口文件
│   └── repositories/      # 数据库操作和数据访问层
│       ├── sqlite_db.py   # SQLite数据库操作
│       └── ...            # 其他数据库操作文件
│
├── infrastructure/        # 基础设施和外部资源
│   ├── restful_api.py     # RESTful接口调用
│   ├── file_io.py         # 文件操作
│   ├── import_export.py   # 数据导入和导出
│   └── ...                # 其他基础设施文件
│
├── utils/                 # 通用工具和帮助函数
│   ├── logging_utils.py   # 日志记录工具
│   ├── exceptions.py      # 通用异常类
│   └── ...                # 其他通用工具文件
│
├── config/                # 配置文件
│   ├── settings.py        # 应用设置和配置
│   └── ...                # 其他配置文件
│
├── main.py                # 项目入口文件
└── README.md              # 项目说明文档
```

实际文件结构

```
│  main.py					# 项目入口文件
│  README.md				# 项目介绍
│
├─application				# 业务逻辑处理
│  │  task_queue.py			# 任务队列管理
│  │
│  └─services
│          evaluation_service.py	# 评测服务
│          optimization_service.py	# 优化逻辑服务
│          speaker_recognition_service.py	# 发音人识别服务
│
├─config					# 配置文件
│      settings.ini			# 配置文件
│
├─docs						# 如你所见，文档
│      目录结构文档.md
│      结构设计文档.md
│      需求分析文档.md
│
├─domain					# 领域模型和业务实体
│  ├─exceptions				# 异常处理
│  │      exceptions.py		# 自定义异常
│  │
│  ├─models					# 领域模型类
│  │      evaluation_result.py	# 评测结果模型
│  │      speaker_info.py	# 发音人信息模型
│  │      voice_data.py		# 语音数据模型
│  │
│  ├─repositories			# 数据库操作和数据访问层
│  │      models.py			# 存放PEEWEE的数据库模型
│  │      repositories.py	# 数据库相关增删改查操作
│  │
│  └─service
│          evaluation_service_protocol.py	# 语音评测协议定义
│          optimization_service_protocol.py # 优化逻辑协议定义
│          speaker_recognition_service_protocol.py	# 发音人识别协议定义
│
├─infrastructure			# 基础设施和外部资源
│      file_io.py			# 文件操作
│      restful_api.py		# RESTful接口调用
│
├─presentation				# UI界面相关代码
│  │  main_window.py		# 主界面相关逻辑
│  │
│  ├─pyuic					# 存放 pyuic转换后生成的py文件
│  │      readme.md
│  │
│  └─uifiles				# 存放 QT Designer 生成的ui文件
│          readme.md
│
└─utils						# 其他工具函数
        logging_utils.py	# 日志记录工具
        init_tools.py		# 初始化程序工具
```

